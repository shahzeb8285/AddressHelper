'use strict';

var expect = require('chai').expect;
var Book = require('../index');

describe('#numFormatter', function() {
    it('should convert single digits', function() {
        var {BSC_TOKENS} = Book;
        console.log(BSC_TOKENS)
    });


});